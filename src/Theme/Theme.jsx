import { createTheme } from "@mui/material/styles";
  //primary main color, we are use for Background gradient;
  let theme=createTheme({
    palette:{
        text:{
            primary:'#28395a',
            secondary:'#ffffff',
            hint:'#fb246a'
        },
         
    }
  })
 

  export default theme;